# T28 — Mimari ve bağımlılık ekranı

**Amaç:** Mimari ve bağımlılık ekranı davranışını kaynak belgenin §6, §10, §11, §12, §29, §55 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `frontend/src/features/architecture/`, `frontend/src/features/dependencies/`, `frontend/tests/architecture/`. Orvant'ta izlenen zorunlu çıktı `frontend/src/features/architecture/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `frontend/src/features/architecture/`, `frontend/src/features/dependencies/`, `frontend/tests/architecture/`; izlenen `frontend/src/features/architecture/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T18, T25, T26

**Somut kabul örneği:** Graf düğümü dosyaya gider; mimari güven ve belirsizlik ile Mermaid/PlantUML seçimi görünür.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-luna / medium. **Gerekçe:** Net UI/sunum veya dokümantasyon sözleşmesi.

**Yeni sohbete başlangıç:** “Görev adı `ASA | T28 | Luna | Mimari ve bağımlılık ekranı`. AI Software Architect `T28` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
