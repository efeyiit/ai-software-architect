# T06 — TypeScript AST çıkarımı

**Amaç:** TypeScript AST çıkarımı davranışını kaynak belgenin §7, §14, §31, §47, §52 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/parsers/typescript/`, `backend/tests/parsers/typescript/`. Orvant'ta izlenen zorunlu çıktı `backend/app/parsers/typescript/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `backend/app/parsers/typescript/`, `backend/tests/parsers/typescript/`; izlenen `backend/app/parsers/typescript/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T01, T03

**Somut kabul örneği:** TS/TSX fixture için export, sınıf, fonksiyon, yöntem, import, çağrı ve kalıtım kaynak konumuyla bulunur.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-sol / medium. **Gerekçe:** Sözleşmeli backend, entegrasyon veya doğrulama işi.

**Yeni sohbete başlangıç:** “Görev adı `Ariadne | T06 | Sol | TypeScript AST çıkarımı`. Ariadne `T06` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
