# T26 — React uygulama kabuğu

**Amaç:** React uygulama kabuğu davranışını kaynak belgenin §4, §5, §6, §29, §47 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `frontend/src/app/`, `frontend/src/components/shell/`, `frontend/src/styles.css`, `frontend/tailwind.config.ts`, `frontend/tests/shell/`. Orvant'ta izlenen zorunlu çıktı `frontend/src/app/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `frontend/src/app/`, `frontend/src/components/shell/`, `frontend/tests/shell/`; izlenen `frontend/src/app/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T01

**Somut kabul örneği:** Login, dashboard ve repository rotaları, sol navigasyon ve erişilebilir dar ekran yerleşimi çalışır.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-luna / low. **Gerekçe:** Net UI/sunum veya dokümantasyon sözleşmesi.

**Yeni sohbete başlangıç:** “Görev adı `Ariadne | T26 | Luna | React uygulama kabuğu`. Ariadne `T26` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
