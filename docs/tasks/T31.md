# T31 — Incremental analiz ve silme/yeniden adlandırma

**Amaç:** Incremental analiz ve silme/yeniden adlandırma davranışını kaynak belgenin §42, §49, §52, §53 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/incremental/`, `backend/tests/incremental/`. Orvant'ta izlenen zorunlu çıktı `backend/app/incremental/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `backend/app/incremental/`, `backend/tests/incremental/`; izlenen `backend/app/incremental/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T10, T20, T22, T23, T25

**Somut kabul örneği:** Git diff değişen, silinen, yeniden adlandırılan dosyaları ve etkilenen ters bağımlıları yeniler; eski Qdrant/bulgu kalıntısı silinir.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-sol / high. **Gerekçe:** Yetki, veri bütünlüğü, dil çözümleme, asenkron akış veya birleşik analiz riski.

**Yeni sohbete başlangıç:** “Görev adı `Ariadne | T31 | Sol | Incremental analiz ve silme/yeniden adlandırma`. Ariadne `T31` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
