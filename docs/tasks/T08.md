# T08 — C# AST çıkarımı

**Amaç:** C# AST çıkarımı davranışını kaynak belgenin §14, §47, §52 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/parsers/csharp/`, `backend/tests/parsers/csharp/`. Orvant'ta izlenen zorunlu çıktı `backend/app/parsers/csharp/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `backend/app/parsers/csharp/`, `backend/tests/parsers/csharp/`; izlenen `backend/app/parsers/csharp/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T01, T03

**Somut kabul örneği:** Namespace, using, sınıf/interface, yöntem, çağrı ve kalıtım fixture ile çıkarılır.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-sol / medium. **Gerekçe:** Sözleşmeli backend, entegrasyon veya doğrulama işi.

**Yeni sohbete başlangıç:** “Görev adı `ASA | T08 | Sol | C# AST çıkarımı`. AI Software Architect `T08` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
