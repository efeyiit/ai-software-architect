# T20 — Chunk, embedding ve Qdrant indeksleme

**Amaç:** Chunk, embedding ve Qdrant indeksleme davranışını kaynak belgenin §23, §24, §25, §26, §27, §47, §52 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/rag/indexing/`, `backend/tests/rag/indexing/`. Orvant'ta izlenen zorunlu çıktı `backend/app/rag/indexing/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `backend/app/rag/indexing/`, `backend/tests/rag/indexing/`; izlenen `backend/app/rag/indexing/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T03, T05, T06, T07, T08, T09

**Somut kabul örneği:** Chunk dosya/satır/SHA ve kullanıcı sahipliğini korur; arama başka kullanıcının özel kodunu getirmez; tekrar indeks idempotenttir.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-sol / high. **Gerekçe:** Yetki, veri bütünlüğü, dil çözümleme, asenkron akış veya birleşik analiz riski.

**Yeni sohbete başlangıç:** “Görev adı `Ariadne | T20 | Sol | Chunk, embedding ve Qdrant indeksleme`. Ariadne `T20` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
