# T01 — Uygulama iskeleti ve analiz sözleşmeleri

**Amaç:** Uygulama iskeleti ve analiz sözleşmeleri davranışını kaynak belgenin §3, §27, §28, §30, §31, §33, §34, §35, §37, §38, §47, §54 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/contracts/`, `backend/tests/contracts/`, `backend/pyproject.toml`, `backend/app/main.py`, `frontend/src/contracts/`, `frontend/tests/contracts/`, `frontend/package.json`, `frontend/tsconfig.json`, `frontend/vite.config.ts`, `docs/architecture-decisions.md`. Orvant'ta izlenen zorunlu çıktı `backend/app/contracts/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** Backend/frontend başlangıç yapılandırması, ortak analiz sözleşmeleri, `docs/architecture-decisions.md`; izlenen `backend/app/contracts/README.md` ve iki tarafta sözleşme testleri.

**Önkoşullar:** Yok; ilk hazır iş.

**Somut kabul örneği:** Aynı bulgu kimliği, dosya konumu, SHA ve kaynak türü API ile istemcide kayıpsız taşınır; kullanıcı kodu talimat olarak işlenmez.

**Hedefli doğrulama:** Backend ve frontend sözleşme testlerinde aynı örnek bulguyu serileştirip geri oku; dosya konumu, SHA, kaynak türü ve hata alanlarını karşılaştır. İki uygulamanın başlangıç derlemesini/çalışmasını doğrula. Komut ve sonucu kısa kanıtla bildir.

**Model:** gpt-6-sol / medium. **Gerekçe:** Sözleşmeli backend, entegrasyon veya doğrulama işi.

**Yeni sohbete başlangıç:** “Görev adı `ASA | T01 | Sol | Uygulama iskeleti ve analiz sözleşmeleri`. AI Software Architect `T01` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
