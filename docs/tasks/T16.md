# T16 — Repository güvenlik analizi

**Amaç:** Repository güvenlik analizi davranışını kaynak belgenin §20, §39, §45, §52 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/analyzers/security/`, `backend/tests/security/`. Orvant'ta izlenen zorunlu çıktı `backend/app/analyzers/security/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `backend/app/analyzers/security/`, `backend/tests/security/`; izlenen `backend/app/analyzers/security/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T05, T06, T07, T08, T09

**Somut kabul örneği:** API key/parola, SQL kurma, JWT doğrulama, eksik auth, hassas log ve env secret kategorileri olası risk olarak konumlu raporlanır.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-sol / high. **Gerekçe:** Yetki, veri bütünlüğü, dil çözümleme, asenkron akış veya birleşik analiz riski.

**Yeni sohbete başlangıç:** “Görev adı `Ariadne | T16 | Sol | Repository güvenlik analizi`. Ariadne `T16` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
