# T05 — Python AST çıkarımı

**Amaç:** Python AST çıkarımı davranışını kaynak belgenin §7, §14, §31, §47, §52 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/parsers/python/`, `backend/tests/parsers/python/`. Orvant'ta izlenen zorunlu çıktı `backend/app/parsers/python/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `backend/app/parsers/python/`, `backend/tests/parsers/python/`; izlenen `backend/app/parsers/python/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T01, T03

**Somut kabul örneği:** Fixture içindeki sınıf, fonksiyon, yöntem, import, değişken, çağrı ve kalıtım konumları bulunur; parse hatası dosyayı çökertmez.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-sol / medium. **Gerekçe:** Sözleşmeli backend, entegrasyon veya doğrulama işi.

**Yeni sohbete başlangıç:** “Görev adı `ASA | T05 | Sol | Python AST çıkarımı`. AI Software Architect `T05` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
