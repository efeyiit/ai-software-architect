# T02 — PostgreSQL şeması ve sahiplik sınırları

**Amaç:** PostgreSQL şeması ve sahiplik sınırları davranışını kaynak belgenin §32, §33, §39 kapsamıyla tamamlamak.

**Tam dosya sahipliği:** `backend/app/database/`, `backend/tests/database/`. Orvant'ta izlenen zorunlu çıktı `backend/app/database/README.md` bu sahiplik içinde oluşturulur. Bu yolların dışındaki ortak sözleşme değişiklikleri proje sorumlusuyla koordine edilir.

**Girdiler:** `docs/source-design.md`, `docs/MASTER-PLAN.md`, ilgili önceki görev çıktıları ve mevcut sözleşmeler.

**Çıktılar:** `backend/app/database/`, `backend/tests/database/`; izlenen `backend/app/database/README.md` ve göreve ait hedefli kontrol kanıtı.

**Önkoşullar:** T01

**Somut kabul örneği:** İki kullanıcının aynı URL için kayıtları ayrıdır; commit ve analiz sonuçları doğru repository sahibine bağlanır.

**Hedefli doğrulama:** Bu davranış için temsilî olumlu/olumsuz fixture veya UI akışı çalıştırılır; sınır/hata hali ve kaynak/SHA/yetki etkisi gerektiğinde incelenir. Sonuç ve kullanılan komut/örnek dosya kartta kısa kanıtla kaydedilir. Testi ayrı göreve erteleme.

**Model:** gpt-6-sol / medium. **Gerekçe:** Sözleşmeli backend, entegrasyon veya doğrulama işi.

**Yeni sohbete başlangıç:** “Görev adı `ASA | T02 | Sol | PostgreSQL şeması ve sahiplik sınırları`. AI Software Architect `T02` kartını uygula. Önce AGENTS.md, Orvant context ve bu kartı oku; yalnız sahip olduğun dosyalarda davranış + hedefli test + belgeyi bitir. Orvant state'i değiştirme; sonuç ve kanıtı proje sorumlusu ana sohbete bildir.”
