# T01 verification — 2026-09-23

## Delivered

- FastAPI bootstrap at `backend/app/main.py`, with `/health` returning `{"status":"ok"}`.
- React/TypeScript bootstrap at `frontend/index.html` and `frontend/src/main.tsx`.
- Python and TypeScript analysis result contracts with one canonical JSON fixture.
- Full repository commit SHA, relative file and inclusive line range, separate
  static/AI source, analysis status, and structured errors.
- Architecture and contract notes in `docs/architecture-decisions.md` and
  `backend/app/contracts/README.md`.

## Checks

Run from `backend/`: `uv sync`, then `uv run pytest -q` and
`uv run python -c "from app.main import app; print(app.title, len(app.routes))"`.
Run from `frontend/`: `pnpm install --frozen-lockfile`, `pnpm test`, and
`pnpm build`.

The canonical fixture round trips without field loss on both sides. Negative
cases reject a bad SHA, unknown source, traversal path, zero or reversed line
range, string line number, extra field, duplicate finding ID, and failed
status without a structured error. Backend tests also exercise `/health`.

The frozen frontend lockfile install passed. Backend: 12 tests passed and
`app` imported with 5 routes. Frontend: 12 tests passed and Vite production
build succeeded. Backend emitted one Starlette deprecation warning from
`fastapi.testclient`.

## Limits

This is a contract and startup foundation. Repository fetching, persisted
analyses, AI calls, and analysis endpoints belong to later tasks. The sample
payload is synthetic. No repository code is executed.

Dependency releases were checked against the official
[PyPI FastAPI](https://pypi.org/project/fastapi/),
[PyPI Pydantic](https://pypi.org/project/pydantic/),
[PyPI Uvicorn](https://pypi.org/project/uvicorn/),
[npm React](https://www.npmjs.com/package/react), and
[npm Vite](https://www.npmjs.com/package/vite) package records before pinning.
